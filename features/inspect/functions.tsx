import { Diagram, Shortcut } from "../spec/diagramSchema";

export function editKeybind(
  keyId: string,
  shortcut: Shortcut,
  setKeyDiagram: (diagramUpdater: (diagram: Diagram) => any) => void,
) {}
